# Dev/test deps. The base API stack (requirements.txt) plus test tooling.
-r requirements.txt
pytest>=8.0
httpx>=0.27   # required by fastapi's TestClient
ruff>=0.6      # lint, run in CI with the default rule set
