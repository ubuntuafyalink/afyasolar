# AfyaSolar AI Engine - API + data-pipeline deps (light; no ML training stack).
# The API boots and /health works with just these. Forecasting additionally
# needs the serving stack (requirements-serve.txt); training needs
# pipeline/train/requirements.txt.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pandas>=2.0
pyarrow>=14.0
numpy>=1.24
requests>=2.31
pyyaml>=6.0
