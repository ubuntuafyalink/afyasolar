# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Data + model artifacts (large / regenerable) - never commit
pipeline/data/raw/
pipeline/datasets/processed/
pipeline/train/outputs/
models/
*.parquet
*.ckpt
*.safetensors

# Large fetched NASA POWER datasets (kept local; regenerable via pipeline/data/).
# Small config/docs (grid_locations.json, *.info.json) stay tracked.
dataset/*.csv
dataset/csv/
dataset/raw/

# HuggingFace / caches
.cache/
